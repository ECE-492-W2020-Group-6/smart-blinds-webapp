import React from "react";
import { Button } from "@material-ui/core";

const App: React.FC = () => {
  return (
    <React.Fragment>
      <Button variant="contained" color="primary">
        Hello World
      </Button>
    </React.Fragment>
  );
};

export default App;
