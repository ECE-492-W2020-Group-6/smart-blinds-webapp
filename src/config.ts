interface IConfig {
  root: string;
  defaultPath: string;
  mainTitle: string;
}

const config: IConfig = {
  root: "/smart-blinds-webapp",
  defaultPath: "/",
  mainTitle: "Smart Blinds"
};

export default config;
